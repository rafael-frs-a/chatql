# File used to simulate something equivalent to Django's `python manage.py shell` and Flask's `flask shell`
# Call it with `ipython -i shell.py`
# Then call `await start_app()`
from src.app import start_app  # noqa
